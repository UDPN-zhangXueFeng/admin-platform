# Dev-only login prefill. Copy these values into .env.development.local.
# The fields are prefilled in development unless the disable flag is exactly "true".
NEXT_PUBLIC_DEV_LOGIN_NAME=your-local-dev-username
NEXT_PUBLIC_DEV_LOGIN_PASSWORD=your-local-dev-password
NEXT_PUBLIC_DISABLE_DEV_LOGIN_PREFILL=false
