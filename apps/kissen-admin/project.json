{
  "name": "@admin-platform/kissen-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/kissen-admin",
  "projectType": "application",
  "tags": ["scope:kissen-admin", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec next build --webpack",
        "cwd": "apps/kissen-admin"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec next dev --webpack",
        "cwd": "apps/kissen-admin"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec next start",
        "cwd": "apps/kissen-admin"
      }
    }
  }
}
